[package]
name = "xai-grok-agentflow"
version = "0.5.1"
edition.workspace = true
license = "Apache-2.0"
description = "Agent-flow policy engine for grok-local: effort behavior policies, turn budgets, label-driven tool packs, plan-then-execute, anchored compaction, and doom-loop escalation. Pure logic — never touches inference."

[dependencies]
regex = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
toml = { workspace = true }
xai-dirs = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
